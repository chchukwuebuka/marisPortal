# Copy this file to `.env.local` and fill in your real values.
# `.env.local` is gitignored, so your real API address never gets committed.

# Base URL of the Marist Admissions API (no trailing slash needed).
# This is just the server address the browser talks to — not a secret key.
NEXT_PUBLIC_API_BASE_URL=http://your-api-host/api
